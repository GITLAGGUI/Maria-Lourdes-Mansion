# Environment variables for deployment
# Add your actual environment variables here before deployment

# Example environment variables (replace with actual values)
# NEXT_PUBLIC_BASE_URL=https://your-domain.netlify.app
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# DATABASE_URL=your_database_connection_string
# API_SECRET_KEY=your_secret_key

# Note: This file should be ignored by git to keep secrets safe
# Make sure to add these variables in Netlify's dashboard under Site Settings > Environment Variables